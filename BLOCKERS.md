# v1.0 Blockers

Anything that couldn't be completed in this session. Empty file is the goal.

## Active

(none — the remaining work in `TODO.md` is just unfinished, not blocked.)

## Notes

The autonomous overnight build's full scope (4 investigation docs + architecture + IPC + 4 foundation modules + 4 Node services + pages.ts extensions + 8 Java helper commands + 17 Node tool handlers + 25 smoke assertions + 10 evaluation questions + 2 benchmarks + manifest update + README rewrite + pack + self-install test) is genuinely larger than one continuous session. The build paused at a clean architectural checkpoint per `PROGRESS.md`. No designs are ambiguous; the remaining work is execution against locked specs in `docs/`.

If the next session encounters anything that genuinely blocks (e.g., a Lucene API mismatch, a Shamela JRE incompatibility, a sql.js version regression), record it here with: what was tried, what's blocking, and a plan to work around or defer.
